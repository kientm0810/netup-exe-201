services:
  postgres:
    image: postgres:16-bookworm
    container_name: netup-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: netup
      POSTGRES_USER: netup
      POSTGRES_PASSWORD: netup
    ports:
      - "${POSTGRES_HOST_BIND:-127.0.0.1}:${POSTGRES_HOST_PORT:-55432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U netup -d netup"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7.4-bookworm
    container_name: netup-redis
    restart: unless-stopped
    ports:
      - "${REDIS_HOST_BIND:-127.0.0.1}:${REDIS_HOST_PORT:-6379}:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  adminer:
    image: adminer:latest
    container_name: netup-adminer
    restart: unless-stopped
    environment:
      ADMINER_DEFAULT_SERVER: postgres
    ports:
      - "${ADMINER_HOST_BIND:-0.0.0.0}:${ADMINER_HOST_PORT:-8080}:8080"
    depends_on:
      postgres:
        condition: service_healthy

  backend-api:
    build:
      context: ./backend
    container_name: netup-backend-api
    restart: unless-stopped
    env_file:
      - ./backend/.env
    environment:
      CORS_ORIGINS: "${CORS_ORIGINS:-http://ec2-13-251-104-233.ap-southeast-1.compute.amazonaws.com,http://13.251.104.233}"
      FRONTEND_BASE_URL: "${PUBLIC_BASE_URL:-http://ec2-13-251-104-233.ap-southeast-1.compute.amazonaws.com}"
      BACKEND_BASE_URL: "${PUBLIC_BASE_URL:-http://ec2-13-251-104-233.ap-southeast-1.compute.amazonaws.com}"
      GOOGLE_REDIRECT_URI: "${PUBLIC_BASE_URL:-http://ec2-13-251-104-233.ap-southeast-1.compute.amazonaws.com}/api/v1/auth/google/callback"
      VNPAY_RETURN_URL: "${PUBLIC_BASE_URL:-http://ec2-13-251-104-233.ap-southeast-1.compute.amazonaws.com}/api/v1/payments/vnpay/return"
    ports:
      - "${BACKEND_HOST_BIND:-127.0.0.1}:${BACKEND_HOST_PORT:-8000}:8000"
    volumes:
      - ./backend:/app
    entrypoint: >
      sh -c "
      echo 'Running database migrations...' &&
      alembic upgrade head &&
      echo 'Seeding development admin if enabled...' &&
      python -m app.scripts.seed_admin &&
      echo 'Starting NetUp API...' &&
      exec uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
      "
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    image: node:22-bookworm
    container_name: netup-frontend
    working_dir: /app
    restart: unless-stopped
    environment:
      COREPACK_ENABLE_DOWNLOAD_PROMPT: "0"
      NODE_OPTIONS: "${NODE_OPTIONS:---dns-result-order=ipv4first}"
      NPM_CONFIG_REGISTRY: "${NPM_CONFIG_REGISTRY:-https://registry.npmjs.org/}"
      PNPM_VERSION: "${PNPM_VERSION:-11.2.2}"
      NEXT_PUBLIC_API_BASE_URL: "${NEXT_PUBLIC_API_BASE_URL:-}"
      NEXT_PUBLIC_NETUP_FACEBOOK_URL: "${NEXT_PUBLIC_NETUP_FACEBOOK_URL:-https://www.facebook.com/netup68}"
    volumes:
      - ./frontend:/app
      - frontend-node-modules:/app/node_modules
    command: ["sh", "/app/docker-entrypoint.sh"]
    healthcheck:
      test: ["CMD-SHELL", "test -f /app/out/index.html"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 20s

  nginx:
    build:
      context: .
      dockerfile: ./ops/production/nginx/Dockerfile
    container_name: netup-nginx
    restart: unless-stopped
    ports:
      - "${NGINX_HOST_PORT:-80}:80"
    volumes:
      - ./ops/production/nginx/netup-static.conf:/etc/nginx/conf.d/default.conf:ro
      - ./frontend:/var/www/frontend:ro
    depends_on:
      frontend:
        condition: service_healthy
      backend-api:
        condition: service_started

volumes:
  postgres-data:
  redis-data:
  frontend-node-modules:
